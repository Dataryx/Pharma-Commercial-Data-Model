# Contributing

- Prefixes: `br_` / `sv_` / `mdm_` / `dim_` / `fct_` / `bridge_` / `mart_` / `sec_`
- Every model declares grain in `meta.grain` and has a uniqueness test
- Business metrics require a test in the same PR
- Synthetic data only — never commit real licensed or patient-identified data
